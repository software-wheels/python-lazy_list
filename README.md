# python-lazy_list
A lazy list implementation in python. Evaluate only the require indexes and only once.
Free to use :)
All code is hand written. No copy pasta.

Examples:
from lazy_list import LazyList

Look in the 'tests' directory for use-case examples.
